package dex.mcgitmaker


import net.fabricmc.loader.api.SemanticVersion

class McMetadataTest {
    static void main(String[] args) {
        //def x = new GitCraft()
        //def v = x.versions.get(SemanticVersion.parse('1.19.1-beta.1'))

        //println Util.Outlet.INSTANCE.outletDatabase

        //x.merged() // has to be called or it endlessly tries to merge them

        //v.remappedJar()
        //v.decompiledMc()

        //Decompiler.decompile(v)
        //x.decompileAllVersions(false)
        //x.updateRepo()
        //v.decompiledMc()
        //Util.updateMcVersionPath(v)

        //keyset is in ascending order, good
    }
}
